import { MessageErrorFormPipe } from './message-error-form-pipe';

describe('MessageErrorFormPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageErrorFormPipe();
    expect(pipe).toBeTruthy();
  });
});
